import 'package:flutter/material.dart';

/// App color palette - CoinMarketCap inspired dark theme
class AppColors {
  // Background colors
  static const Color primaryBackground = Color(0xFF0A0E27);
  static const Color secondaryBackground = Color(0xFF14172B);
  static const Color cardBackground = Color(0xFF1C1F3A);
  static const Color surfaceBackground = Color(0xFF22264A);

  // Accent colors
  static const Color primaryAccent = Color(0xFF00D4FF); // Cyan/Blue
  static const Color secondaryAccent = Color(0xFF6C63FF); // Purple

  // Success/Positive (Green)
  static const Color success = Color(0xFF00C853);
  static const Color successLight = Color(0xFF26A69A);
  static const Color successDark = Color(0xFF00A043);

  // Error/Negative (Red)
  static const Color error = Color(0xFFFF5252);
  static const Color errorLight = Color(0xFFFF6B6B);
  static const Color errorDark = Color(0xFFE53935);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF); // White
  static const Color textSecondary = Color(0xFF8E92A8); // Gray
  static const Color textTertiary = Color(0xFF5A5E78); // Darker gray
  static const Color textDisabled = Color(0xFF3A3E58);

  // Border colors
  static const Color border = Color(0xFF2A2E4A);
  static const Color borderLight = Color(0xFF363A58);

  // Chart colors
  static const Color chartGreen = Color(0xFF00E396);
  static const Color chartRed = Color(0xFFFF4560);
  static const Color chartBlue = Color(0xFF008FFB);
  static const Color chartYellow = Color(0xFFFEB019);
  static const Color chartPurple = Color(0xFF775DD0);

  // Overlay colors
  static const Color modalBarrier = Color(0x80000000);
  static const Color shimmerBase = Color(0xFF2A2E4A);
  static const Color shimmerHighlight = Color(0xFF363A58);

  // Status colors
  static const Color warning = Color(0xFFFFB020);
  static const Color info = Color(0xFF2196F3);
}
